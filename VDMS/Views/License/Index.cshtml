@model IEnumerable<VDMS.ViewModel.LicenseViewModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="row">
    <div class="col-sm-12">
        <div class="card">
            <h5 class="card-header">Vehicles License</h5>
            <div class="card-body">
                <table class="table" id="vdataTable">
                    <thead>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.vehicle.VehicleNo)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Renewal)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Expiry)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.DsOffice)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Amount)
                            </th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.vehicle.VehicleNo)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Renewal)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Expiry)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.DsOffice)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Amount)
                                </td>
                                <td>
                                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }, new { @class = "btn btn-warning" })
                                    @Html.ActionLink("Details", "Edit", new { id = item.Id }, new { @class = "btn btn-info" })
                                    @Html.ActionLink("Delete", "Delete", new { id = item.Id }, new { @class = "btn btn-danger" })
                                </td>
                            </tr>
                        }
                    <tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section scripts
{
    <script>
        $(document).ready(function () {
            $('#vdataTable').DataTable();
        });
    </script>
}
